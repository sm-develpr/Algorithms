#include <iostream>
#include <vector>
#include <limits>
#include <algorithm>
/*
 * Задача №115071. Заправки
На прямой дороге расположено 𝑛 заправок, 𝑖-я заправка расположена на расстоянии 𝑥𝑖 от начала дороги.

Петя находится на первой заправке и хочет доехать до 𝑛-й. Объем бака Петиной машины 𝑏 литров. Петина машина проезжает один километр на одном литре бензина. На 𝑖-й заправке бензин стоит 𝑐𝑖 рублей за литр. Исходно бак Петиной машины пуст.

В соответствии с правилами техники безопасности разрешается заправлять машину только если ее бак пуст, таким образом если Петя собирается заправляться на некоторой заправке, он должен полностью израсходовать ранее заправленный бензин в момент подъезда к ней, при этом разрешено заливать неполный бак бензина.

Помогите Пете доехать от первой заправки до последней за минимальную сумму денег.

Входные данные
В первой строке входного файла находятся два целых числа 𝑛 и 𝑏 — количество заправок и размер Петиного бака (2≤𝑛≤1000, 1≤𝑏≤109). Следующие 𝑛 строк содержат по два числа: 𝑥𝑖 и 𝑐𝑖 (1≤𝑥1<𝑥2<...<𝑥𝑛≤109, 1≤𝑐𝑖≤109).

Выходные данные
Выведите одно число — минимальное число денег, необходимое Пете, чтобы доехать от первой заправки до последней. Если совершить такой переезд на Петиной машине невозможно, выведите −1.
 */
using namespace std;

int main() {
    int n;
    long long b;
    cin >> n >> b;

    vector<long long> x(n), c(n);
    for (int i = 0; i < n; ++i) {
        cin >> x[i] >> c[i];
    }

    vector<long long> dp(n, 1e18); // Инициализируем массив очень большим значением
    dp[0] = 0; // Начиная с первой заправки

    // Проходим по всем заправкам
    for (int i = 0; i < n; ++i) {
        if (dp[i] == 1e18) continue; // Если на эту заправку не добраться, пропускаем

        // Проверяем все последующие заправки
        for (int j = i + 1; j < n; ++j) {
            long long distance = x[j] - x[i]; // Расстояние до следующей заправки

            if (distance > b) break; // Если расстояние больше объема бака, то дальше идти нет смысла

            // Если до j-й заправки добраться можно
            long long liters_needed = distance; // Нужно столько же литров, сколько километров
            long long cost = liters_needed * c[i]; // Сколько потратим на заправке i

            dp[j] = min(dp[j], dp[i] + cost); // Обновляем минимальные затраты для j-й заправки
        }
    }

    // Результат: минимальная сумма денег для достижения n-й заправки
    cout << (dp[n - 1] == 1e18 ? -1 : dp[n - 1]) << endl;

    return 0;
}
